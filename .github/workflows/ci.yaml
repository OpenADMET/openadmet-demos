name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  schedule:
    - cron: "0 0 * * 0"  # weekly run

jobs:
  test:
    name: Test on ${{ matrix.os }}, Python ${{ matrix.python-version }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [macOS-latest, ubuntu-latest]
        python-version: ["3.12"]

    steps:
      # Checkout repo
      - uses: actions/checkout@v4

      # Display system info for debugging
      - name: Additional info about the build
        shell: bash
        run: |
          uname -a
          df -h
          ulimit -a

      # Setup micromamba and create environment
      - uses: mamba-org/setup-micromamba@v1
        with:
          environment-file: openadmet-demos.yaml
          environment-name: openadmet-demos
          condarc: |
            channels:
              - conda-forge
          create-args: >-
            python=${{ matrix.python-version }}

      # Activate environment and run notebooks
      - name: Activate environment and run notebooks
        shell: bash -l {0}
        run: |
          micromamba activate openadmet-demos
          echo "Environment packages:"
          micromamba list
          echo "Running notebooks with pytest-nbmake..."
          pytest --nbmake --maxfail=1 --disable-warnings **/*.ipynb

      # Optionally run CodeCov
      - name: CodeCov
        uses: codecov/codecov-action@v5.4.2
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          name: codecov-${{ matrix.os }}-py${{ matrix.python-version }}
